import React,{useState,useEffect,useRef}from'react';import{Link,useNavigate,useLocation}from'react-router-dom';import{Search,Heart,ShoppingBag,X,Menu,ArrowRight}from'lucide-react';import{useStore}from'../store/useStore';import{products}from'../data/products';
const Navbar:React.FC=()=>{const[scrolled,setScrolled]=useState(false),[searchOpen,setSearchOpen]=useState(false),[menuOpen,setMenuOpen]=useState(false),[searchQuery,setSearchQuery]=useState('');const[searchResults,setSearchResults]=useState<typeof products>([]);const searchRef=useRef<HTMLDivElement>(null);const navigate=useNavigate();const location=useLocation();const{cartCount,wishlist}=useStore();useEffect(()=>{const h=()=>setScrolled(window.scrollY>20);h();window.addEventListener('scroll',h);return()=>window.removeEventListener('scroll',h)},[]);useEffect(()=>{const h=(e:MouseEvent)=>{if(searchRef.current&&!searchRef.current.contains(e.target as Node)){setSearchOpen(false);setSearchQuery('')}};document.addEventListener('mousedown',h);return()=>document.removeEventListener('mousedown',h)},[]);useEffect(()=>{if(searchQuery.trim().length>1){const q=searchQuery.toLowerCase();setSearchResults(products.filter(p=>p.name.toLowerCase().includes(q)||p.brand.toLowerCase().includes(q)||p.category.toLowerCase().includes(q)||p.tags?.some(t=>t.includes(q))).slice(0,5))}else setSearchResults([])},[searchQuery]);useEffect(()=>{setMenuOpen(false)},[location.pathname]);const navLinks=[{label:'Home',path:'/'},{label:'Shop',path:'/shop'}];const isActive=(p:string)=>p==='/'?location.pathname==='/':location.pathname.startsWith(p);return <><nav className={`fixed top-0 left-0 right-0 z-50 transition-all duration-500 ${scrolled?'bg-white/85 backdrop-blur-xl border-b border-zinc-200/70 shadow-[0_8px_30px_rgba(0,0,0,.06)]':'bg-white/95 backdrop-blur-sm border-b border-zinc-100'}`}><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"><div className="flex items-center justify-between h-[68px]"><Link to="/" className="group flex items-center gap-2 font-syne font-bold text-xl tracking-[-.04em] text-zinc-900"><span className="w-2 h-2 rounded-full bg-amber-500 group-hover:scale-150 transition-transform"/>KAIXY</Link><div className="hidden md:flex items-center gap-9">{navLinks.map(l=><Link key={l.path} to={l.path} className={`nav-link ${isActive(l.path)?'text-zinc-900 font-semibold':''}`}>{l.label}</Link>)}</div><div className="flex items-center gap-1"><button aria-label="Search" onClick={()=>setSearchOpen(!searchOpen)} className="w-10 h-10 rounded-full flex items-center justify-center text-zinc-600 hover:bg-zinc-100 hover:text-zinc-900 transition-all">{searchOpen?<X size={18}/>:<Search size={18}/>}</button><Link aria-label="Wishlist" to="/wishlist" className="relative w-10 h-10 rounded-full flex items-center justify-center text-zinc-600 hover:bg-zinc-100 hover:text-zinc-900 transition-all"><Heart size={18}/>{wishlist.length>0&&<span className="absolute top-0 right-0 w-[17px] h-[17px] bg-amber-600 text-white text-[9px] font-bold flex items-center justify-center rounded-full animate-pulse">{wishlist.length}</span>}</Link><Link aria-label="Cart" to="/cart" className="relative w-10 h-10 rounded-full flex items-center justify-center text-zinc-600 hover:bg-zinc-100 hover:text-zinc-900 transition-all"><ShoppingBag size={18}/>{cartCount()>0&&<span className="absolute top-0 right-0 w-[17px] h-[17px] bg-zinc-900 text-white text-[9px] font-bold flex items-center justify-center rounded-full">{cartCount()}</span>}</Link><button aria-label="Menu" onClick={()=>setMenuOpen(!menuOpen)} className="md:hidden w-10 h-10 rounded-full flex items-center justify-center text-zinc-600 hover:bg-zinc-100 transition-all ml-1">{menuOpen?<X size={18}/>:<Menu size={18}/>}</button></div></div></div>{searchOpen&&<div ref={searchRef} className="border-t border-zinc-100 bg-white/95 backdrop-blur-xl px-4 sm:px-6 lg:px-8 py-4 animate-[pageFade_.3s_ease-out]"><div className="max-w-3xl mx-auto relative"><div className="flex items-center gap-3 border border-zinc-200 rounded-full px-5 py-3 focus-within:border-zinc-900 focus-within:shadow-lg transition-all"><Search size={16} className="text-zinc-400"/><input autoFocus placeholder="Search products, brands, categories..." value={searchQuery} onChange={e=>setSearchQuery(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&searchQuery.trim()){navigate(`/shop?search=${encodeURIComponent(searchQuery)}`);setSearchOpen(false);setSearchQuery('')}}} className="flex-1 text-sm text-zinc-900 placeholder-zinc-400 focus:outline-none bg-transparent"/>{searchQuery&&<button onClick={()=>setSearchQuery('')} className="text-zinc-400 hover:text-zinc-900"><X size={14}/></button>}</div>{searchResults.length>0&&<div className="absolute top-full left-0 right-0 bg-white border border-zinc-100 rounded-2xl shadow-2xl mt-2 z-50 overflow-hidden animate-[pageFade_.25s_ease-out]">{searchResults.map(product=><button key={product.id} onClick={()=>{navigate(`/product/${product.id}`);setSearchOpen(false);setSearchQuery('')}} className="w-full flex items-center gap-3 px-4 py-3 hover:bg-zinc-50 text-left transition-colors"><img src={product.image} alt={product.name} className="w-10 h-12 object-cover bg-zinc-100 rounded-md"/><div className="flex-1 min-w-0"><p className="text-sm font-medium text-zinc-900 truncate">{product.name}</p><p className="text-xs text-zinc-500">{product.category}</p></div><span className="text-sm font-semibold text-zinc-900">${product.discount?(product.price*(1-product.discount/100)).toFixed(0):product.price}</span></button>)}<button onClick={()=>{navigate(`/shop?search=${encodeURIComponent(searchQuery)}`);setSearchOpen(false);setSearchQuery('')}} className="w-full px-5 py-4 text-xs font-semibold text-amber-600 hover:bg-amber-50 text-center border-t border-zinc-100 flex items-center justify-center gap-2">See all results <ArrowRight size={13}/></button></div>}</div></div>}{menuOpen&&<div className="md:hidden border-t border-zinc-100 bg-white px-5 py-5 animate-[pageFade_.3s_ease-out]"><div className="flex flex-col gap-2">{navLinks.map(l=><Link key={l.path} to={l.path} className={`text-base font-medium rounded-xl px-4 py-3 transition-colors ${isActive(l.path)?'bg-zinc-950 text-white':'text-zinc-600 hover:bg-zinc-50'}`}>{l.label}</Link>)}</div></div>}</nav><div className="h-[68px]"/></>};export default Navbar;
