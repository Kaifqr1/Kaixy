import React from 'react';import{Star}from'lucide-react';interface Props{rating:number;size?:number;showCount?:boolean;count?:number}const StarRating:React.FC<Props>=({rating,size=14,showCount,count})=><div className="flex items-center gap-1.5"><div className="flex gap-0.5">{[1,2,3,4,5].map(s=><Star key={s} size={size} className={s<=Math.round(rating)?'text-amber-500 fill-amber-500':'text-zinc-200 fill-zinc-200'}/>)}</div>{showCount&&count!==undefined&&<span className="text-xs text-zinc-400">({count})</span>}</div>;export default StarRating;
