import React from 'react';
import { Link } from 'react-router-dom';
import { Heart, ShoppingBag, Trash2, ArrowRight } from 'lucide-react';
import { useStore } from '../store/useStore';
import ProductCard from '../components/ProductCard';
import toast from 'react-hot-toast';
const WishlistPage:React.FC=()=>{const{wishlist,toggleWishlist,addToCart}=useStore();const handleRemoveAll=()=>{wishlist.forEach(p=>toggleWishlist(p));toast('Wishlist cleared',{icon:'🗑️',style:{fontFamily:'Inter, sans-serif',fontSize:'14px'}})};const handleMoveAllToCart=()=>{wishlist.forEach(p=>addToCart(p,1));wishlist.forEach(p=>toggleWishlist(p));toast.success('All items moved to cart!',{icon:'🛍️',style:{fontFamily:'Inter, sans-serif',fontSize:'14px'}})};if(!wishlist.length)return <div className="min-h-[70vh] flex items-center justify-center px-4"><div className="text-center fade-in"><div className="w-20 h-20 bg-zinc-50 border border-zinc-100 rounded-full flex items-center justify-center mx-auto mb-5"><Heart size={28} className="text-zinc-300"/></div><h2 className="font-syne font-bold text-2xl text-zinc-900 mb-2">Your wishlist is empty</h2><p className="text-zinc-400 text-sm mb-8 max-w-xs mx-auto">Save items you love and come back to them anytime.</p><Link to="/shop" className="btn-primary inline-flex items-center gap-2">Explore Shop<ArrowRight size={15}/></Link></div></div>;return <div className="min-h-screen bg-white"><div className="border-b border-zinc-100 bg-zinc-50"><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8"><div className="flex items-end justify-between"><div><h1 className="font-syne font-bold text-2xl sm:text-3xl text-zinc-900 tracking-tight">Wishlist</h1><p className="text-sm text-zinc-400 mt-1">{wishlist.length} saved item{wishlist.length!==1?'s':''}</p></div><div className="flex items-center gap-3"><button onClick={handleMoveAllToCart} className="hidden sm:flex items-center gap-2 text-sm font-medium text-zinc-600 border border-zinc-200 px-4 py-2 hover:border-zinc-900 hover:text-zinc-900"><ShoppingBag size={14}/>Move All to Cart</button><button onClick={handleRemoveAll} className="flex items-center gap-2 text-sm text-zinc-400 hover:text-red-500"><Trash2 size={14}/><span className="hidden sm:inline">Clear All</span></button></div></div></div></div><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8"><div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 sm:gap-6">{wishlist.map(product=><ProductCard key={product.id} product={product}/>)}</div><div className="mt-8 sm:hidden text-center"><button onClick={handleMoveAllToCart} className="btn-primary inline-flex items-center gap-2"><ShoppingBag size={15}/>Move All to Cart</button></div></div></div>};export default WishlistPage;
