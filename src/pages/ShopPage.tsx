import React,{useMemo}from'react';import{useSearchParams}from'react-router-dom';import ProductCard from'../components/ProductCard';import{products}from'../data/products';
const ShopPage:React.FC=()=>{const[params,setParams]=useSearchParams();const category=params.get('category')||'All';const search=params.get('search')||'';const filtered=useMemo(()=>products.filter(p=>(category==='All'||p.category.toLowerCase()===category.toLowerCase())&&(!search||[p.name,p.brand,p.category,...(p.tags||[])].some(x=>x.toLowerCase().includes(search.toLowerCase())))),[category,search]);return <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10 page-fade"><div className="flex flex-col sm:flex-row sm:items-end justify-between gap-5 border-b border-zinc-100 pb-8"><div><p className="section-label">Collection</p><h1 className="font-syne text-3xl font-bold">Shop</h1><p className="text-sm text-zinc-400 mt-2">{filtered.length} products</p></div><select value={category} onChange={e=>setParams({category:e.target.value})} className="select-field sm:w-48"><option>All</option>{['Fashion','Footwear','Electronics','Accessories','Home'].map(x=><option key={x}>{x}</option>)}</select></div>{search&&<p className="text-sm text-zinc-500 mt-6">Search results for “{search}”</p>}<div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 sm:gap-6 mt-8">{filtered.map(p=><ProductCard key={p.id} product={p}/>)}</div>{!filtered.length&&<div className="py-24 text-center text-zinc-400">No products found.</div>}</div>};export default ShopPage;
